# --- Web framework ----------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12        # FastAPI 의 UploadFile 파싱에 필요

# --- Image / background removal ---------------------------------------------
Pillow==10.4.0
numpy==1.26.4
rembg==2.0.59                   # u2net / isnet 등 배경 제거 모델 래퍼
onnxruntime==1.19.2             # rembg 가 사용하는 추론 엔진

# --- Storage (옵션) ---------------------------------------------------------
firebase-admin==6.5.0            # FIREBASE_STORAGE_BUCKET 가 설정됐을 때만 사용
python-dotenv==1.0.1

# --- Dev / test -------------------------------------------------------------
pytest==8.3.3
httpx==0.27.2                    # FastAPI TestClient 가 내부적으로 사용
